import Position from "./Position";

const DungeonLevelGenerate = (z) => {
    const dungeon = { 
        dungeonWidth: 10,
        dungeonHeight: 10
    };
    return dungeon;
}

export default DungeonLevelGenerate;